# 大厂 AI 应用 / Agent 岗 · 工程化八股与情景题调研

> **背景**：基于 DeepSeek、月之暗面、智谱、字节、阿里云、腾讯、B站、拼多多、TikTok、Momenta 等公司 2025–2026 年真实面经整理，聚焦**工程化八股**与**开放性情景题**——这些正是"调 API 的 demo 项目"覆盖不到的线上实战问题。
> **用法**：每家公司先看画像（考什么风格），再按主题刷工程八股，最后专门练情景题（面试官最爱"换着姿势盘问"的就是这类）。
> **配合**：`AI技术岗-手撕与八股清单.md` 管基础手撕与通用八股；本文档管"生产级追问"与"系统设计题"。文末有对照你 cookhero 项目的差距分析。

---

## 目录

- [Part 1 · 各公司面试画像](#part-1--各公司面试画像)
- [Part 2 · 工程化八股（按主题）](#part-2--工程化八股按主题)
  - [2.1 记忆与上下文的生产级追问](#21-记忆与上下文的生产级追问)
  - [2.2 工具 / MCP / Skill](#22-工具--mcp--skill)
  - [2.3 并发 / 存储 / 分布式](#23-并发--存储--分布式)
  - [2.4 推理与部署](#24-推理与部署)
  - [2.5 RAG 生产化](#25-rag-生产化)
  - [2.6 评测体系](#26-评测体系)
  - [2.7 稳定性与安全](#27-稳定性与安全)
- [Part 3 · 情景设计题（一道一题）](#part-3--情景设计题一道一题)
- [Part 4 · 手撕/笔试真题补充](#part-4--手撕笔试真题补充)
- [Part 5 · 面试趋势 & 对照你的项目](#part-5--面试趋势--对照你的项目)
- [来源列表](#来源列表)

---

# Part 1 · 各公司面试画像

> 面经是个人视角，不代表全公司统一标准；但"风格"有普遍参考价值。

## 1.1 DeepSeek：代码即正义，专问线上实战

**画像**（来源：[36氪《程序员面试背了十年的八股文，被DeepSeek废了》](https://www.36kr.com/p/3893858235972482)、[DeepSeek 大模型算法岗笔试面经](https://www.nowcoder.com/discuss/913482312753418240)）：
- **前两轮纯代码面**（约 70 分钟），自我介绍只给三四十秒，直接揪简历项目连环追问到"具体改了哪块逻辑、效果提升多少百分比"。
- 不考传统八股（HashMap、JVM 那套全废），**问的几乎全部来自线上实战**：
  - 会话记忆用 message window 还是 token window？线上为什么这么选？（客服对话 vs 长文档场景）
  - Redis 存会话，过期策略怎么定？半小时清还是保留七天？用户突然回来发现聊天没了体验怎么办？
  - 有没有遇到过**并发会话冲突**？手机和网页同时往一个会话写消息，上下文怎么拼？会不会读到脏数据？
  - 长期记忆用向量库存，记忆从几千条涨到几十万条，**检索越来越不精准**怎么优化？
  - 线上故障排查：追到具体改了哪块逻辑、效果提升了多少百分比才结束。
- **三面不问代码**，聊认知与判断：你怎么看 Agent 这个赛道？它最大的瓶颈在哪？如果让你从零带一条线，你第一步做什么？
- **笔试真题**（[牛客面经](https://www.nowcoder.com/discuss/913482312753418240)）：完整 MHA（不能只写框架）、DPO 完整训练流程推导、MoE 通信开销计算与负载不均衡分析、PagedAttention + 投机解码的工程实现、DSpark 推理加速机制推导、DeepSeek-V4 KV Cache 压缩（MLA）原理与显存计算、多模态视觉原语数学建模、MoE 无辅助损失负载均衡推导。

**给你的信号**：不要背八股，要把项目里的每个决策讲成"我遇到了 X 问题，对比了 A/B，选了 B，因为 C，线上验证是 D"。

## 1.2 月之暗面：一个点换六种姿势盘问

**画像**（来源：[月之暗面 Agent开发岗凉经](https://www.nowcoder.com/discuss/909210322701942784)、[月之暗面开发面试](https://www.nowcoder.com/discuss/800129080900272128)）：
- **风格**：典型技术流，一个问题会连续追问直到确认你"真懂还是背的"。该面经里"记忆"一个点被换了六种姿势盘问。
- **记忆管理深挖链**（完整 27 题面经，见 Part 2.1）：短期记忆怎么实现 → 什么时候触发总结 → 是每轮都总结吗 → **第 11 轮怎么处理（增量还是全量）** → 前 10 轮原始上下文要不要存 → 长期记忆什么时候检索 → 长短期记忆是否同时注入。
- **工程实战题**：50 个工具 token 膨胀怎么办（渐进式披露，省 89%）→ 工具返回数据过大怎么办 → 幻觉分层防御（输入/推理/上下文/输出四层）→ 检索审计日志（三层归因：召回/排序/生成）。
- **底层追问**：余弦相似度和欧氏距离在归一化前提下数学等价，为什么业界都选余弦？
- **算法题**：LeetCode 143 重排链表——面试官明确"不能用数组解，要看指针操作"。
- **开发岗面经**：etcd 和 k8s 的 etcd 区别、健康检查与 k8s 节点状态检查为什么不能互相替代、管理面/数据面与健康检查的关系、手撕**可配置并发的请求队列调度系统**、TCP 拥塞控制了解吗。

**给你的信号**：记忆/上下文是你项目有但会被往死里问的模块——把"触发阈值、增量 vs 全量、原始数据存不存"这几个点全部想清楚。

## 1.3 智谱：Infra 向，并行与手撕硬核

**画像**（来源：[智谱一面](https://www.nowcoder.com/feed/main/detail/44f8d7d738634e21b13298330a7fea28)、[智谱大模型算法岗面经](https://blog.csdn.net/CSDN_430422/article/details/147092839)）：
- Infra 岗会拷打**并行策略**：你们场景怎么用 TP/DP/EP？TP 和 DP 应用在哪个权重计算位置？两者如何协同？（MoE 的 FFN 用 EP）
- **动态批处理**（continuous batching）调度了解吗？
- **KV Cache 管理策略**：vLLM 的 PagedAttention，为什么 page 不能过大也不能过小？（过大 → 内部碎片浪费；过小 → block table 开销 + 跨块访问多）
- **手撕 MoE Dispatch**：输入 token 的 gating_scores 和 k，返回 k 个专家各自分配到的 token_id（用 C++ 写）。

**给你的信号**：投 Infra/推理向岗位要补 TP/DP/EP、vLLM 调度、MoE 路由；应用岗至少把 PagedAttention 的 page size trade-off 讲清楚。

## 1.4 字节：手撕快准狠 + 记忆/RAG 连环问

**画像**（来源：[字节 AI Agent 实习面经](https://www.nowcoder.com/feed/main/detail/8a553bb6ea8445d0b0abe11e87614cea)、36氪）：
- 一面：手撕要求速度和干净程度极高，过不了直接挂；项目深挖 + 一两道算法题。
- 二面：模型结构追到数学原理层——你提 RoPE，就一路追问到旋转矩阵推导；SFT/RLHF/DPO、LoRA 原理。
- 三面交叉面：工程系统——DeepSpeed ZeRO 并行策略、DP 和 DDP 区别、多卡训练的硬件利用率估算。
- **Agent 实习一面 20 题**（代表性）：ReAct 与 Plan 模式区别与适用场景；上下文超过限制怎么处理；压缩保留哪些关键信息、怎么判断可丢；**如何避免压缩后模型忘记用户重要需求**；**路径震荡**（反复尝试）原因与优化；完整记忆系统设计；Context 与长期 Memory 区别；记忆提取/存储/检索；**为什么记忆管理可以交给小模型**；MCP 是什么、调用流程；大量工具如何避免 Prompt 过长；**工具返回数据过大怎么办**；**Prompt Cache 是什么、怎么提高命中率**；Multi-Agent 职责划分与通信；**多个 Agent 同时改同一个文件怎么避免冲突**；车载场景为什么用小模型初召。
- 算法题：判断平衡二叉树。

**给你的信号**：手撕速度 + 记忆/RAG/工具的每个"生产级细节"都要能现场推导。

## 1.5 阿里云：Agent 开发 = Skill/Harness/AI Coding

**画像**（来源：[阿里云 agent 开发 8.18 一面](https://www.nowcoder.com/feed/main/detail/42ba29ab006d4bb793112eb263d18f6a)、[阿里校招 Agent 开发一面凉经](https://blog.csdn.net/stone_tmp/article/details/161122267)）：
- 平常怎么用 AI 辅助编码？你用的 skill 有哪些？**知名 skill 内部是怎么写的**？自己写过 skill 吗，写的时候注意什么？
- **Claude-code 和 deepseek-harness 的区别**？看过源码吗？**写好 harness 哪些是必须的**？
- Agent 工具调用有哪些方式？**怎么保障 agent 按需调用工具**（而不是乱调）？
- 为什么有子 agent 的设计？什么时候用 agent 合理？agent 之间通信方式？
- **设计一个 agentloop 要注意什么**？
- 当场 AI-coding 测试（用 agent 写代码）。

**给你的信号**：你正在用 DeepSeek Harness——这是加分项也是考点，去读 harness 源码结构（Goal/Todo/Workflow 分层），能讲清"为什么需要规范工程"。

## 1.6 腾讯 wxg：项目拷打 + RAG 细节

**画像**（来源：[腾讯 wxg 大模型应用开发岗面经](https://www.nowcoder.com/discuss/769357001628397568)）：
- 为什么用多智能体不用单智能体？
- 了解哪些大模型应用框架（你项目的竞品）？
- RAG 知识库怎么搭的、怎么分片？
- 如何让大模型理解医学名词（领域术语优化）？
- 知识库很大时**接口速度问题**考虑过吗？（不只 RAG 检索优化，还有数据层）
- RAG 评估的具体数值是多少？（背过没用，要真做过）
- 36氪补充：腾讯面试官逐级升高，一面考模型理解力（BERT/LoRA/位置编码），二面聊底层优化（微调经验/DeepSpeed/增量预训练），三面系统设计与潜力判断。

**给你的信号**：项目拷打深度 > 八股；RAG 评测要真跑过、报得出数字。

## 1.7 哔哩哔哩：广度覆盖 + 基建考察

**画像**（来源：[哔哩哔哩 AI 应用岗 agent 开发一面](https://www.nowcoder.com/feed/main/detail/0df78025653d492d86cc7b7273c2a0c2)、[B站 Agent 一面/二面](https://www.nowcoder.com/discuss/919630453387063296)）：
- **一面 18 题**：Agent 核心模块 vs 传统 Chatbot 区别；Planning/Memory/Tool Use/执行职责；意图识别（规则/小模型/LLM 各自的适用场景）；Zero-shot vs Few-shot 与标注数据少时怎么稳；RAG 完整流程；切块策略与 RecursiveCharacterTextSplitter 实现逻辑、中文文档注意什么；**RAG 召回差怎么定位——优先查 Embedding/Chunk/Query Rewrite/Hybrid/Rerank 哪个**；Embedding 选型影响；LangChain 核心组件与 LCEL；Function Calling 流程与模型如何判断调用工具；工具描述膨胀优化；Prompt 组织（System/Few-shot/CoT 各干什么）；幻觉工程优化；**重复调用工具/无法结束的保护机制**；短期/长期记忆与记忆检索流程。
- **二面/基建向**：项目云端部署怎么做；**cgroup 了解吗**；**A2A 协议**；CI/CD；RocksDB **Compacting 策略**；负载均衡怎么做；怎么校验 Agent 的确定性结果；Hooks 怎么校验工作；**badcase 怎么确定优化方向**；Workflow 与 Agent 的区别。
- 手撕：合并重叠区间（LeetCode 56）。

**给你的信号**：广度题（意图识别、RAG 定位顺序）和基建题（cgroup/A2A/CI-CD）都要有基本认知。

## 1.8 拼多多：API 失败排查 + 多角色记忆

**画像**（来源：[拼多多提前批 AI Agent 一面面经](https://www.nowcoder.com/discuss/919634965879324672)及热榜相关帖）：
- **使用大模型 API 过程中调用失败有哪些原因？如何判断请求是否成功？**（状态码/重试/超时/内容校验）
- LLM 调用异常怎么**问题定位**？排查流程是什么？
- 除了接口限流，调用大模型还会遇到哪些问题？怎么解决？
- 上下文记忆不足导致模型不理解之前信息，有哪些优化方案？
- **多个 AI 角色同时参与交互，如何设计和管理不同角色的记忆？用户信息和角色信息如何区分存储？**
- 多轮对话怎么保证准确获取历史上下文，同时避免上下文过长影响效果？
- 算法：相同的树（LeetCode 100）；另一场面经：合并重叠区间。
- 相关：会议转写项目（ASR 选型对比、专有名词/同音字处理、隐私传输存储访问安全、中心化 vs 去中心化架构）。

**给你的信号**：API 失败分类与排查流程（错误码/超时/重试分类）要形成"排查方法论"而不是零散知识点。

## 1.9 TikTok（字节海外）：工程基础换新皮 + 场景题

**画像**（来源：[TikTok Agent 工程师面试总结](https://www.nowcoder.com/feed/main/detail/a9b91d99c9154a0da8876eaf56183930)）：
- **老八股换新皮肤**：SSE 基本必问（大模型流式输出）；网络排错（tcpdump/Wireshark 排查大模型 API 调用超时根因）；多线程/协程并发打印、协程池管理。
- **RAG 进化版**：PDF 和复杂表格怎么解析、高保真语义切分；元数据提取；Query Rewriting（HyDE、意图重写）；GraphRAG（图谱+向量）；多通道查询衡量；Rerank 接入、上下文压缩、**溯源到文档片段**。
- **Skill vs MCP**：各自优势？为什么实际中 Skill 比 MCP 用得多？
- **Workflow/DAG/Graph/State** 基础概念；为什么生产环境经常要**自研编排引擎**（ReAct/Plan-and-Execute 转化成状态机）。
- **Agent 生产难点**：死循环调用、幻觉导致工具调错、Token 爆了、性能损耗怎么看、降级与熔断、模型挂了业务怎么兜底。
- **LLM 评测**：自动化评测 Pipeline；指标量化（准确率/幻觉率/召回率/TTFT/TPS/API 成本）；LLM-as-a-Judge 怎么写评测 Prompt 才能客观稳定。
- **场景设计题**：跨 Agent 调度和意图切换（用户查订单突然要看直播怎么处理）；**跨平台 Agent 调用，每个平台授权时间有限，怎么安排调度**。

**给你的信号**：SSE、tcpdump、状态机、Skill/MCP 是新增量，务必补齐。

## 1.10 Momenta / 其他

- **Momenta Agent 开发一面**（来源：[牛客面经](https://www.nowcoder.com/discuss/800129080900272128)相关推荐）：设备遥测数据、维修文档、历史工单**三类异构数据怎么统一检索**（文档→混合检索、工单→结构化字段抽取、遥测→时序库，先解析问题再并行查询，统一转成带来源/时间/可信等级的证据对象）；**如何证明故障诊断不是模型猜的**（诊断必须绑定可验证证据）。
- **小米**（相关推荐）：ReAct 与 Plan-and-Execute 区别（"被问烂的第一题"）。
- **淘天/阿里/浪潮**（来源：[llm-interview 仓库](https://github.com/yuyouyu32/llm-interview/blob/main/5-agent/5.2%20application.md)）：3 个工具 + 高频请求延迟优化；用户理解阶段做什么；设计 Agent Router 的轻量方案。

---

# Part 2 · 工程化八股（按主题）

> 这些是"生产级追问"，你的 demo 项目大概率没遇到，但面试必问。每题给出**面经原题 → 考察点 → 参考答案要点**。

## 2.1 记忆与上下文的生产级追问

### 2.1.1 会话记忆用 message window 还是 token window？线上为什么这么选？（DeepSeek）
**考察点**：选型不是背定义，是要讲清场景与代价。
- message window：按条数存最近 N 条。适合轮数多但每轮短的场景（客服对话）。
- token window：按 token 预算存，超限截断。适合每轮内容很长的场景（长文档/代码库分析）。
- 关键话术："没有标准答案，取决于业务——选错了会怎样（消息窗口在长文档场景会把窗口瞬间撑爆；token 窗口在短轮对话场景会浪费预算在早期长消息上）"。

### 2.1.2 记忆触发的"具体数值策略"和"原子操作"是什么？（月之暗面）
**考察点**：Demo 只说"超了就总结"，生产要讲数值和并发安全。
- **双阈值**：软阈值（如接口上限 70%）异步生成摘要并缓存；硬阈值（90%）同步阻塞强制总结。
- **步数触发**：工具调用超过 N 步还没结果 → 强制总结，"帮模型抬头看方向"。
- **原子性**：先完整拷贝当前上下文副本，在副本上总结，再**原子替换**历史部分——绝不能在并发线程里改到一半发请求。

### 2.1.3 已做 10 轮总结，第 11 轮怎么处理？增量还是全量？（月之暗面·必背）
**考察点**：增量摘要 vs 全量重算的复杂度与代价。
- **增量叠加**：`S_1-10 + D_11` 直接拼接发给模型，不是把 1~11 轮全部重算。
- 全量重算是 O(n²) 且 token 消耗 10 倍以上。
- 陷阱：如果拼完又到阈值，需要对 `S_1-10 + D_11` 做新摘要生成 `S_1-11` 并丢弃 `S_1-10`——**本质是分层摘要树**（每一层是对上一层的压缩）。

### 2.1.4 前 10 轮都变成总结了，原始上下文还要不要？（月之暗面）
**考察点**：容易答错成"不需要"。
- 原始上下文**必须持久化存储，但不发送给模型**。三个用途：
  1. **审计溯源**：模型答错时能回放"它当时看到了什么"；
  2. **摘要重建**：摘要质量差时可以重新生成；
  3. **长期记忆检索**：摘要压缩丢掉的用户关键指令，原始数据里还有，可按需检索找回。

### 2.1.5 是每一轮都要做总结吗？（月之暗面）
**考察点**：成本意识。
- 每轮总结三个问题：计算开销爆炸（每轮多一次 LLM 调用）；信息衰减加速（摘要再摘要，损失指数级）；延迟不可接受（每句话等 2~3 秒）。
- 只在触发条件满足时做，平时就是追加。

### 2.1.6 长期记忆是每轮都注入吗？长短期记忆同时注入吗？（月之暗面）
- 短期记忆是**常驻上下文**，不用"注入"，它就在那儿。
- 长期记忆是**按需动态注入**：先走意图路由，判断"需要查历史"才去向量库检索 Top-K 拼进前缀。
- 同时注入会塞满无关信息浪费 token 且干扰。

### 2.1.7 长期记忆几万条后检索越来越不精准，怎么优化？（DeepSeek·最考功底）
**考察点**：向量检索随规模退化的真实问题。
- 参考答案要点（面经级）：
  1. **分层记忆**：高频/近期的走向量检索，长尾/低频的走关键词或结构化字段过滤；
  2. **先路由再检索**：意图路由/元数据过滤（用户 id、时间窗、类型）缩小候选集；
  3. **记忆条目去重与合并**：相似记忆聚合（"我是 Java 开发"和"我做支付系统"合并成画像条目），控制条目总量；
  4. **召回质量改进**：换更强 embedding、加 rerank、top-k 后按时间/重要度加权；
  5. **写侧治理**：提取时做质量分/类型分，低质记忆不进库；定期清理过期/冲突记忆（遗忘机制）。

### 2.1.8 并发会话冲突：手机和网页同时往一个会话写消息怎么办？（DeepSeek）
**考察点**：多端写同一会话的并发控制。
- 参考答案要点：会话消息用**追加写 + 版本号/游标**（乐观锁），冲突时以版本号为准重读合并；写 Redis 用 Lua 脚本保证原子性；两端的消息按**服务端时间序 + 客户端消息 id 去重**；读侧用快照（读时拷贝），写侧串行化（单会话单 writer）。

### 2.1.9 Redis 会话过期策略怎么定？（DeepSeek）
- 过期时间 = 业务对"用户可能回来说话"的容忍度 + 存储成本权衡：短（30 分钟）省内存但用户回来发现聊天没了体验差；长（7 天）体验好但成本高。
- 工程化：**分层 TTL**（活跃会话滑动续期、冷会话降级存 DB、摘要保留更久）；记住"2 月 30 号清理日志"的典故——定期清理要防边界 bug。

### 2.1.10 压缩后模型忘了用户的重要需求怎么办？（字节）
- 参考答案要点：
  1. **关键信息保底**：压缩前识别"用户明确约束/目标/偏好"（限制类、目标类）单独提取进"不可丢区"（常驻 system/摘要前缀）；
  2. **摘要模板化**：固定"已完成/未完成/当前卡点/重要约束"四段，约束段永不丢；
  3. **可检索兜底**：摘要丢了还能从原始数据/长期记忆检索找回；
  4. **重要性分级压缩**：低价值闲聊先丢，高价值指令后丢。

### 2.1.11 为什么记忆管理、上下文压缩可以交给小模型？（字节）
- 小模型优势：便宜（压缩是高频后台任务）、快（毫秒级）、不占用主模型宝贵的推理上下文；这类任务"理解语义要点"的难度远低于"最终回答"，小模型够用。
- 你的项目里：fast 层模型做意图识别/查询改写就是这个思路（`config.yml` 分层模型）。

## 2.2 工具 / MCP / Skill

### 2.2.1 50 个工具 token 膨胀：工具描述吃掉一半上下文怎么办？（月之暗面·渐进式披露）
- 一个完整工具 schema 约 200~5000 token，50 个工具光描述就 6000~15000 token。
- **渐进式披露（Progressive Disclosure）**：第一轮只加载精简列表（每个工具一行，约 20 token），模型选中后再加载 3 个完整 schema。示例：50×20 + 3×400 = 2200 token，对比 50×400 = 20000，省 89%。
- 额外收益：干扰项少了，工具选择准确率反而上升。
- 进阶：**工具路由**（向量检索 top-k 工具再注入）是业界主流方案。

### 2.2.2 工具返回数据过大，超过上下文窗口怎么办？（字节/月之暗面）
- 参考答案要点：截断（保留头部关键字段）/ 结构化抽取（只回传字段而非全文）/ 摘要化（工具结果先过一遍小模型）/ 分页取用（按需再查）；统一在工具层做"返回值预算"，与上下文压缩联动。

### 2.2.3 MCP 是什么？Agent 通过 MCP 调用工具的流程？（字节）
- MCP = Model Context Protocol，统一大模型与外部工具/数据源的**标准化协议**（类比"AI 世界的 USB-C"）。
- 流程：Client 发现 Server → Server 暴露 tool/resource/prompt 列表 → 工具 schema 注入 LLM → 模型生成 tool_call → Client 按协议转发执行 → 结果回喂。
- 追问（TikTok）：**Skill vs MCP**——Skill 更偏"可复用指令/工作流封装"（跨工具编排+经验沉淀），MCP 更偏"协议标准化"（工具接入）；实际生产 Skill 用得多因为价值在编排与沉淀，不只是连接。

### 2.2.4 大量工具时怎么按需调用，避免模型乱调？（阿里云/字节）
- 参考答案要点：权限矩阵（能调哪些工具）；渐进式披露/工具路由（只暴露候选）；描述质量（什么场景用）；调用前意图分类（规则/小模型先挡）；调用后校验 + 失败结构化回馈；危险工具 HITL 审批。

### 2.2.5 Function Calling 参数生成失败常见原因？怎么提高结构化输出可靠性？（240 题库 L104）
- 失败原因：冷门工具 name 拼错、复杂嵌套 schema 缺字段、多工具同参数名填错、JSON 截断/格式坏。
- 提高可靠性：schema 精简（少嵌套）；few-shot 示例（给一个真实调用样例）；JSON 修复流水线 + schema 校验 fail-fast + 失败返回结构化错误让模型重试；必要时用小模型/规则做参数后校验（L123/L124：LLM 参数纠错，注意防"纠错过度/语义漂移"）。

## 2.3 并发 / 存储 / 分布式

### 2.3.1 TP / DP / EP 并行策略，应用在哪个位置，怎么协同？（智谱）
- DP：数据并行，每卡一份完整模型，切 batch——作用于全部层，梯度 AllReduce。
- TP：张量并行，把单个大矩阵（Attention 的 QKV/FFN）按维度切开分到多卡——作用于**大稠密层**（Attention + 标准 FFN），通信是每层 AllReduce/AllGather。
- EP：专家并行，MoE 的 FFN 按专家切分，token 经 All-to-All 路由到对应专家。
- 协同：MoE 模型通常是 Attention/共享层用 TP(+DP)，MoE FFN 用 EP；TP 组内是高频小通信（NVLink），EP 是跨节点 All-to-All。
- 追问（36氪）：DeepSpeed ZeRO 三个阶段（优化器状态/梯度/参数切分）；DP vs DDP（DDP 是数据并行的一种实现，梯度 AllReduce；ZeRO 进一步切分冗余状态）；多卡训练硬件利用率估算（算力利用率 MFU、通信占比）。

### 2.3.2 etcd 和 k8s 的 etcd 区别？健康检查 vs 节点状态检查？（月之暗面）
- etcd 是分布式 KV（Raft 共识）；k8s 用它存集群状态（节点/资源/控制器期望状态），是"集群的数据库+注册中心"。
- 健康检查（应用自报）与节点状态检查（kubelet 上报资源/可调度性）职责不同：前者保应用可用性，后者保调度正确性，不能互相替代。
- 映射到 Agent 系统：Agent 服务健康检查 = 应用层探活；节点/实例状态 = 调度层。

### 2.3.3 管理面 / 数据面 与健康状态检查的关系？（月之暗面）
- 控制面（管理面）负责配置/策略/状态机（如审批决策、路由规则）；数据面负责实际业务流量（LLM 调用、工具执行）。
- 健康检查应同时覆盖两面：控制面挂了影响新决策，数据面挂了影响存量请求。

### 2.3.4 cgroup / 限流 / 背压？（B站/淘天）
- cgroup：Linux 资源隔离（CPU/内存/IO），容器化的基础——Agent 服务的多租户资源隔离可类比。
- 限流 + 背压：高并发下控制进入系统的请求速率（令牌桶/漏桶），防止 LLM API 被限流、下游被打爆；背压 = 上游感知下游过载主动减速。

### 2.3.5 SSE 流式：断线、重连、并发？（TikTok/B站）
- SSE 事件流：FastAPI StreamingResponse 逐块推送；事件类型区分（token/工具调用/审批请求）。
- 断线恢复：前端记录已消费游标，重连续传（你项目 `hooks/useAgent.ts` 已做）。
- tcpdump/Wireshark 排查 API 超时：抓包看 TCP 重传、TLS 握手、首字节延迟，定位是网络、网关还是模型侧。

## 2.4 推理与部署

### 2.4.1 PagedAttention：page 大小为什么不能过大也不能过小？（智谱/vLLM）
- 过大 → 请求间**内部碎片**浪费显存，浪费的页面无法被别的请求用；
- 过小 → block table 元数据开销大、跨块访问次数多、每次分配开销高；
- 本质是"碎片率 vs 管理开销"的 trade-off；vLLM 默认按 token 数/头数算一个经验值，面试要能说出这个权衡。

### 2.4.2 MoE 通信开销与负载不均衡（DeepSeek 笔试）
- All-to-All Dispatch/Combine 载荷估算；延迟带宽模型 `T = 启动延迟 + 载荷/带宽`；小 batch 延迟项突出，大 batch 带宽与倾斜关键。
- 负载不均衡三指标：均值/最大值、变异系数 std/mean、capacity 溢出丢弃率。
- 缓解：辅助均衡损失、专家偏置（无辅助损失：偏置只影响 Top-k 选择、不影响组合权重，按 batch 窗口更新）、容量约束、随机路由、拓扑感知放置。

### 2.4.3 投机解码的工程实现（DeepSeek 笔试）
- 草稿模型生成 γ 个候选 → 目标模型并行验证 → 贪心接受一致前缀 / 采样用拒绝采样保持目标分布 → 全接受可额外多吃一个 token。
- 收益看端到端：草稿成本 < 节省的目标模型串行步数才算赢；工程难点：tokenizer 兼容、KV 回滚、接受长度变化、低接受率退化为普通解码。

### 2.4.4 KV Cache 压缩（MLA/GQA/量化）（DeepSeek 笔试）
- 标准公式：`2 × L × n_kv_heads × head_dim × seq × batch × bytes`。
- GQA 降头数、量化降字节、MLA 用低秩 latent 投影恢复 K/V（用计算换显存带宽）。
- 要能算一道显存题（如 L=32, kv_heads=8, d=128, seq=4096, fp16 → 32×8×128×4096×2×2 ≈ 1GB/序列）。

### 2.4.5 动态批处理（continuous batching）？（智谱/TikTok）
- 静态 batching 等整批完成，请求长短不一导致浪费；动态批处理让请求随时加入/完成腾位，最大化 GPU 利用率。
- 是 vLLM 吞吐优势的核心之一，配合 PagedAttention。

## 2.5 RAG 生产化

### 2.5.1 召回效果差，优先查哪个环节？（B站·定位顺序题）
- 标准排查顺序：**Query 质量（Rewrite）→ Chunk 策略 → Embedding 选型 → 检索方式（Hybrid）→ Rerank → Top-K 截断**。
- 用指标定位：context_recall 低而 precision 高 → 排序/截断问题（调 top_k、rerank）；都低 → 召回问题（换 embedding/切块/改写）。

### 2.5.2 复杂文档解析：PDF、表格、扫描件（TikTok/B站/腾讯）
- 难点：表格结构化、多栏、扫描件 OCR、公式。
- 方案：OCR（版面分析 + 识别）、表格转 markdown/HTML 结构保留、按版面/标题语义切分而非纯长度；元数据提取（文档名/章节/来源）参与过滤与溯源。

### 2.5.3 Query Rewriting 的进阶：HyDE、意图重写、多通道查询（TikTok）
- 基础改写：多轮指代消解、口语→检索式。
- HyDE：先让 LLM 生成假设性回答文档，再用它的 embedding 去检索（无标注数据下的伪相关反馈）。
- 多通道：同一 query 多个改写版本并行检索后融合。

### 2.5.4 GraphRAG（TikTok/240 题库）
- 向量检索管语义相似，GraphRAG 管**实体关系多跳**（"A 和 B 什么关系"）；社区摘要支持全局性问题。
- 适用：需要跨实体推理、关系查询的场景；代价：建图成本高。

### 2.5.5 多源异构数据统一检索（Momenta）
- 三类数据不能直接进同一个向量库：文档→混合检索；结构化工单→抽取字段（型号/故障/症状/结果）；遥测→时序库按时间范围查询。
- 检索时先解析问题中的实体（设备/时间/故障码/症状），**并行查询各数据源**，结果统一转成带来源/时间/可信等级的**证据对象**再重排。
- 防幻觉：**结论必须绑定可验证证据**（引用到具体片段/记录），不能"看起来像"。

### 2.5.6 RAG 作为 Agent 的 Tool（TikTok/字节）
- RAG 工具化：查询改写、检索、rerank 都在工具内部完成，返回带引用的结构化结果；注意工具返回的 token 预算。
- 检索审计日志：每次 query 记录"原文 / 召回 Top-20 id+分数 / rerank Top-5 id+分数 / 最终入上下文片段"——线上 badcase 三层归因（召回/排序/生成）。（月之暗面原题）

## 2.6 评测体系

### 2.6.1 Agent/LLM 评测指标怎么量化？（TikTok/240 题库）
- 模型侧：准确率、幻觉率、召回率、hit@k（意图识别为什么用 Hit@3 不用 Hit@1——给后续槽位填充留余地）。
- 服务侧：TTFT（首字延迟）、TPS（生成速率）、P95/P99、API 成本/token 数。
- 评测方式：人工标注 / LLM-as-a-Judge / 线上业务指标（点击、完成率），三者组合。

### 2.6.2 LLM-as-a-Judge 怎么写评测 Prompt 才客观稳定？（TikTok）
- 要点：给评分标准（维度拆开打分，不要一个总分）；给参考答案/事实锚点；随机打乱顺序、多次采样取均值；避免位置偏差与长度偏差（限制输出长度）；用 rubrics + few-shot 校准。

### 2.6.3 badcase 怎么归因？（B站/240 题库 L45）
- 全链路 trace（traceId 串联）→ 定位到具体节点：意图识别错了？检索没召回？rerank 排掉了？模型没用对上下文？工具调用失败了？
- 三层归因法（月之暗面）：召回率问题 / 排序问题 / 生成问题，一层层查。
- 回归门禁：黄金任务集 + baseline 对比，下降即拦截（你项目已做 `scripts/run_agent_evaluation.py`）。

### 2.6.4 自动化 Prompt 迭代（AutoResearch）闭环？（240 题库 L244）
- 构建"自动运行实验 → badcase 分析 → prompt 自动优化"闭环：评测集（Recall@K/F1）→ 失败样本聚类 → 生成修正 prompt → 回归验证 → 上线灰度。降低对人工经验的依赖。

## 2.7 稳定性与安全

### 2.7.1 Agent 死循环/路径震荡怎么办？（字节/B站）
- 路径震荡 = 模型在错误工具间反复横跳或重复尝试。
- 参考答案要点：迭代上限 + 成本熔断（你项目已有）；**停滞检测**（连续 N 轮工具调用无新信息 → 打断）；失败计数熔断（同一工具连续失败 N 次强制换路）；"成功但没进展"的识别（观察轨迹里工具结果是否新增信息）；反思注入（让模型回顾"我试过什么、为什么失败"）；Plan-and-Execute 中间校验。

### 2.7.2 模型挂了/API 全挂，业务怎么兜底？（TikTok/字节）
- 降级链（fast→normal→vision，你项目已有）；缓存兜底（检索结果缓存直接答）；规则回退（意图明确时走确定性流程）；明确告知用户"服务暂不可用"；熔断保护下游。

### 2.7.3 怎么校验 Agent 的确定性结果？（B站）
- 结构化输出 schema 校验 fail-fast；工具调用参数校验；引用校验（引用的文档必须真实存在）；规则约束后处理（金额/时间格式）；Hooks（关键节点前后置校验钩子）。

### 2.7.4 Prompt Injection / 越狱的工程防御？（240 题库 L150）
- 区别：注入是"通过输入篡改指令"，越狱是"绕过对齐限制"。
- 防御：用户输入检测 + 工具返回内容检测（双层，你项目已做）；权限最小化；危险操作 HITL；输出侧 guardrails（NeMo Guardrails 风格，你项目 `app/security/guardrails/`）。

---

# Part 3 · 情景设计题（一道一题）

> 开放性系统设计题，面试官听"思考过程"多于"结论"。每题结构：**原题（公司）→ 考察点 → 参考答题框架**。

## 3.1 Agent 推理链路 3 个工具 + 高频请求，整体延迟高，怎么优化？（淘天）

**考察点**：延迟优化的完整工具箱。
**答题框架**：
1. **并行化**：无依赖工具并行调用（asyncio.gather），有依赖的用 DAG；每个工具设超时 + 降级。
2. **少调工具**：工具路由（规则/小模型先判）只调必要的；高频中间结果缓存（query/doc/embedding 三级）。
3. **模型侧**：规划用小模型、最终回答用大模型；KV Cache/动态批处理/流式输出；必要时投机解码。
4. **数据侧**：缩小 top_k/窗口；连接复用（HTTP/2 keep-alive）。
5. **成本侧**：prompt 压缩、控制 max_tokens。
6. **观测**：全链路 trace 盯 p95/p99；限流 + 背压防抖动。
> 一句话收尾："串行改并行，重任务用小模型和缓存，服务侧批处理 + KV + 流式，配合超时降级限流，端到端看 p99。"

## 3.2 工具返回数据量过大，超过上下文窗口，怎么办？（字节/月之暗面）

见 2.2.2。答题时补一句"要分工具类型：搜索返回可以截头部摘要，DB 查询可以结构化字段化，长文档可以分页按需取"。

## 3.3 多个 Agent 同时修改同一个文件，怎么避免冲突？（字节）

**考察点**：分布式写冲突。
**答题框架**：文件级锁（分布式锁/Redis lock）；**git 式乐观并发**（版本号/内容哈希，冲突时合并或让用户选）；写队列串行化（单文件单 writer）；职责划分（不同 Agent 改不同文件/模块，从源头避免）；冲突检测 + 报告而不是静默覆盖。

## 3.4 车载 Agent 场景，为什么用小模型初步召回而不是直接调大模型？（字节）

**考察点**：成本/延迟/带宽的现实约束 + 分层模型思想。
**答题框架**：车载算力/网络受限 → 小模型本地初召（毫秒级、零网络）过滤大部分请求；只有初召候选不确定/需要复杂推理才上行大模型；这也是"快模型 + 慢模型"分层路由的典型场景（你项目 fast/normal 分层同理，只是方向相反）。

## 3.5 多个 AI 角色同时交互，用户信息和角色信息怎么区分存储？（拼多多）

**考察点**：多租户 + 多角色的记忆隔离。
**答题框架**：数据模型分三层——用户层（user_id 维度，跨角色共享：偏好/画像）、角色层（role_id 维度，角色自己的人设/状态/上下文）、会话层（session_id 维度，短期窗口）；检索时先路由到正确的作用域；权限上角色只能读写自己作用域 + 只读用户共享层。

## 3.6 跨平台 Agent 调用，每个平台授权时间有限，怎么安排调度？（TikTok）

**考察点**：受限资源的任务调度。
**答题框架**：任务按"授权窗口"排优先级（过期前必须执行完的）；授权到期前预刷新/续期；长任务分片（在授权窗口内完成一部分，保存进度）；调度器维护平台授权日历（类似租约/令牌）；失败时记录断点，授权恢复后续跑。

## 3.7 用户在查订单，突然想看直播了，跨 Agent 意图切换怎么处理？（TikTok）

**考察点**：Agent 间的状态交接。
**答题框架**：意图路由检测切换 → 当前 Agent 保存"完成到一半的任务状态/上下文摘要"（持久化）→ 交接给目标 Agent（携带必要的上下文快照）→ 目标 Agent 完成后可回切或并行保留原任务进度；关键是**状态与上下文的显式移交**，而不是会话级联的重新开始。

## 3.8 设计一个 agentloop，要注意什么？（阿里云）

**考察点**：对 Agent 循环工程化的完整认知。
**答题框架**：
1. **循环控制**：max_iterations、终止条件（无 tool_call / 达到目标 / 成本熔断）；
2. **消息协议**：tool_calls 与 tool 结果按 tool_call_id 配对回喂，格式稳定；
3. **错误处理**：工具失败 → 结构化错误（error_code/retryable/suggestion）让模型自纠；失败计数熔断；
4. **状态管理**：messages 是状态变量，控制膨胀（窗口+摘要）；
5. **可观测**：每轮轨迹落盘（Thought/Action/Observation/工具结果）；
6. **事件流**：SSE 逐步推送，支持审批挂起、中断恢复；
7. **安全**：权限矩阵、HITL、注入检测。

## 3.9 你怎么看 Agent 这个赛道？它最大的瓶颈在哪？从零带一条线，第一步做什么？（DeepSeek 三面）

**考察点**：认知与判断力，没有标准答案。
**答题框架**：赛道（从"对话"到"执行"，AI 从建议者变执行者）→ 瓶颈（可靠性/评测闭环/长任务一致性/成本，选一个讲透）→ 从零带线第一步（先定义评测与黄金任务集，再搭最小闭环，再补可观测/安全——"先建评测体系再写功能"）。参考你项目 INTERVIEW_PREP.md 的 D2（重新做会先做什么）。

## 3.10 知识库很大时接口速度慢，除了 RAG 检索优化还考虑什么？（腾讯 wxg）

**考察点**：数据层/存储层的工程视野。
**答题框架**：数据量大 → 向量库分片/索引调优（HNSW 参数）；缓存（精确 + 语义两级）；文档生命周期（TTL/增量更新，240 题库 L61）；连接池/异步；检索范围收窄（先元数据过滤再向量检索）；必要时离线路由（把"哪些库值得查"先算好）。

## 3.11 手撕：可配置并发的请求队列调度系统（月之暗面）

**考察点**：并发控制 + 可配置策略（信号量/线程池 + 动态调整）。
**答题框架**：核心是一个带最大并发数的任务队列——`asyncio.Semaphore` 或线程池 + 有界队列；可配置并发数（动态调整信号量/池大小）；支持优先级/超时/拒绝策略；面试官追问"怎么用策略调整并发数量"时，答：按响应时间反馈（慢则降并发防雪崩）、按配额（账号/模型维度）、类似 **TCP 拥塞控制**的思想（慢启动/拥塞避免/AIMD）。

## 3.12 手撕：MoE Dispatch（智谱）

**考察点**：路由选择 + 分配，能现场写 C++/Python。
**答题框架**：对每个 token 取 gating_scores 前 k 个专家 → 按专家分组收集 token_id → 返回 `{expert_id: [token_ids]}`；注意 top-k 的排序稳定性、容量上限（超出丢弃/重路由）。

## 3.13 如何证明一次故障诊断不是模型根据相似文本猜的？（Momenta）

**考察点**：可验证性/证据绑定。
**答题框架**：诊断结论必须绑定**可验证证据**（具体文档片段/工单记录/遥测数据点，带来源、时间、可信等级）；回答中引用 [n] 且后处理校验引用真实性；评测用 grounding truth 验证"结论是否由证据推出"而非相似度；高风险结论要求二次确认（HITL）。

---

# Part 4 · 手撕/笔试真题补充

> 大厂真题中出现过的、除第一份清单外的代码题。注意：**多数考的是"数据结构意识"而非 API 熟练度**（月之暗面重排链表是典型）。

| 题目 | 公司 | 考点/提示 |
|---|---|---|
| LeetCode 143 重排链表 | 月之暗面 | **必须链表原地操作**：快慢指针找中点 → 反转后半 → 交替合并；不能用数组。输入输出要自己写 |
| LeetCode 110 平衡二叉树 | 字节 | 自底向上返回高度，避免重复计算 |
| LeetCode 100 相同的树 | 拼多多 | 递归/迭代双解 |
| LeetCode 56 合并重叠区间 | B站/拼多多 | 排序后贪心合并，讲复杂度 O(n log n) |
| MoE Dispatch（gating→专家分配 token） | 智谱 | top-k 选择 + 按专家分组，C++ 手写 |
| 可配置并发请求队列 | 月之暗面 | 信号量/线程池 + 动态并发调整 |
| 完整 MHA（含 mask/数值稳定） | DeepSeek 笔试 | 不能只写四个 Linear，讲清形状/掩码语义/contiguous |
| DPO 训练流程推导 | DeepSeek 笔试 | 从 KL 约束 RLHF 出发，消去奖励模型，写出损失与梯度方向 |
| AI coding 现场（用 agent 写代码） | 阿里云 | 考察工具链熟练度 + 代码质量 |

---

# Part 5 · 面试趋势 & 对照你的项目

## 5.1 趋势总结（来自面经共性）

1. **从"会背"到"会做"**：DeepSeek 带头，问题全部来自线上实战（并发写会话、记忆检索退化、过期策略），背题无效；字节/腾讯则是"标准答案 + 追到数学层"的混合。
2. **工程能力是硬门槛**：SSE、并发、限流熔断、多端一致性、存储选型——TikTok 总结帖原话"别觉得做 Agent 就不考工程了，该拷打的一样拷打"。
3. **记忆与上下文是 Agent 岗的第一深挖点**：月之暗面 27 题、字节 20 题里约 1/3 都围绕记忆/上下文/压缩。
4. **RAG 从"切分+向量化"进化到生产化**：复杂文档解析、HyDE/GraphRAG、多源异构、检索审计、RAG as Tool。
5. **评测闭环成为高频追问**：指标数值要真跑过，LLM-as-a-Judge 要会写，badcase 要能归因。
6. **开放性情景题占比上升**：延迟优化、授权调度、意图切换——考"系统设计思维"。

## 5.2 对照你的 cookhero：能答什么 / 缺什么 / 怎么补

### ✅ 你的项目已经覆盖（面试可深讲）
- ReAct 循环、Tool Registry、结构化错误（error_code/retryable/suggestion）——字节/月之暗面/拼多多的工具题大半能答。
- 记忆系统（双阈值压缩、增量摘要、长期记忆提取+回忆）——**月之暗面的记忆链问题你的实现直接对口**，但要补"第 11 轮增量/全量、原始数据存不存、原子替换"这几个深问的答案（你当前是滑动窗口+摘要，需确认摘要是否增量、是否有分层树）。
- RAG 全链路（混合检索 RRF、重排、query rewrite、双层缓存）——B站/腾讯的 RAG 题能答。
- 重试降级链、成本熔断、HITL、注入双层检测、traceId 轨迹——稳定性/安全题能答。
- 评测闭环（工具 SLO、任务完成率、RAGAS grounding truth、回归门禁）——比多数面经作者强。

### ⚠️ 你有但会被"往死里问"的点（必须补深）
1. **记忆触发与增量语义**：你的压缩是"双阈值 + 增量摘要"，请准备好：软/硬阈值具体数值、摘要与后续对话如何增量融合、并发下替换的原子性、摘要树结构。
2. **并发会话**：你的会话是单端写，DeepSeek 问"双端并发写同一会话"——补：乐观锁/版本号/追加写方案。
3. **记忆检索退化**：你的是 2-gram 关键词检索，规模大了会退化——补：分层记忆、先路由再检索、条目合并去重、遗忘机制。
4. **多角色记忆**：Subagent 有独立工具集和 system prompt，但"多角色同时交互的记忆隔离"没做——补：user/role/session 三层存储设计。

### ❌ 你完全没覆盖（新增量，优先级从高到低）
| 主题 | 优先级 | 怎么补 |
|---|---|---|
| 渐进式披露 / 工具路由（50 工具 token 膨胀） | P0 | 你的 AgentHub 有工具 schema，补一个"精简列表→按需加载完整 schema"设计，面试直接讲 |
| 工具返回过大处理 | P0 | 补截断/摘要/结构化抽取策略 |
| Prompt Cache 命中率 | P0 | 了解 vLLM prefix caching，讲"system prompt 放前面不动" |
| 路径震荡/停滞检测 | P0 | 补"连续 N 轮无新信息 → 打断/反思"的设计 |
| SSE 断线重连细节 | P1 | 你前端已做断点恢复，补 tcpdump 排查超时的方法论 |
| 状态机（Workflow/DAG） | P1 | 你的 harness 有 workflow，把"为什么生产要自研编排引擎"讲成状态机 |
| Skill vs MCP | P1 | 你项目有 MCP 接入，补 Skill 概念对比 |
| TP/DP/EP、PagedAttention、MoE | P2 | 投应用岗了解即可；投 Infra 岗必须深学 |
| HyDE / GraphRAG / 多源异构检索 | P2 | 了解概念，能讲适用场景 |

## 5.3 两周补强计划（针对本清单）

1. **Day 1–3**：把 Part 2 的 2.1 记忆 11 题全部对照你的 `app/context/compress.py` + `app/memory/` 写成"我的实现 + 深问答案"。
2. **Day 4–6**：把 Part 2 的 2.2 工具/MCP/Skill、2.7 稳定性对着你的 `app/agent/` 过一遍，每个考点补一个"我项目哪里对应/哪里没做"。
3. **Day 7–9**：Part 3 情景题全部口头答一遍（每题 3 分钟），重点练 3.1/3.8/3.13。
4. **Day 10–12**：Part 4 手撕题过一遍（尤其重排链表和 MoE dispatch）；读一下 DeepSeek Harness 源码结构（阿里云会问）。
5. **Day 13–14**：模拟面试——让对方拿本清单的"深挖链"（记忆、RAG 定位、工具膨胀）连续追问。

---

# 来源列表

| 公司/主题 | 链接 |
|---|---|
| DeepSeek 面试哲学与线上题 | [36氪：程序员面试背了十年的八股文，被DeepSeek废了](https://www.36kr.com/p/3893858235972482) |
| DeepSeek 大模型算法岗笔试面经 | [牛客](https://www.nowcoder.com/discuss/913482312753418240) |
| 月之暗面 Agent开发岗凉经（27 题） | [牛客](https://www.nowcoder.com/discuss/909210322701942784) |
| 月之暗面开发面试 | [牛客](https://www.nowcoder.com/discuss/800129080900272128) |
| 智谱一面（Infra/并行/MoE） | [牛客](https://www.nowcoder.com/feed/main/detail/44f8d7d738634e21b13298330a7fea28) |
| 智谱大模型算法岗面经 | [CSDN](https://blog.csdn.net/CSDN_430422/article/details/147092839) |
| 字节 AI Agent 实习面经（20 题） | [牛客](https://www.nowcoder.com/feed/main/detail/8a553bb6ea8445d0b0abe11e87614cea) |
| 阿里云 agent 开发 8.18 一面 | [牛客](https://www.nowcoder.com/feed/main/detail/42ba29ab006d4bb793112eb263d18f6a) |
| 阿里校招 Agent 开发一面凉经 | [CSDN](https://blog.csdn.net/stone_tmp/article/details/161122267) |
| 腾讯 wxg 大模型应用开发岗 | [牛客](https://www.nowcoder.com/discuss/769357001628397568) |
| 哔哩哔哩 AI 应用岗 agent 开发一面 | [牛客](https://www.nowcoder.com/feed/main/detail/0df78025653d492d86cc7b7273c2a0c2) |
| B站 Agent 一面/二面 | [牛客](https://www.nowcoder.com/discuss/919630453387063296) |
| 拼多多 AI Agent 岗两轮面经 | [牛客](https://www.nowcoder.com/discuss/919634965879324672) |
| TikTok Agent 工程师面试总结 | [牛客](https://www.nowcoder.com/feed/main/detail/a9b91d99c9154a0da8876eaf56183930) |
| llm-interview 仓库（公司标注的 Agent 面经） | [GitHub yuyouyu32/llm-interview](https://github.com/yuyouyu32/llm-interview/blob/main/5-agent/5.2%20application.md) |
| AI Agent 面试真题库（182+ 题，附答题框架） | [GitHub SuperGODOG/ai-agent-interview-240](https://github.com/SuperGODOG/ai-agent-interview-240) |
| 大模型算法岗实习面经（DeepSeek/Qwen 技术报告） | [GitHub Junvate/LLM-Algorithm-Intern-Guide](https://github.com/Junvate/LLM-Algorithm-Intern-Guide) |

> ⚠️ 面经为求职者个人视角，题目与答案可能有偏差；请以多来源交叉验证 + 官方文档为准。部分牛客链接可能需登录查看。
